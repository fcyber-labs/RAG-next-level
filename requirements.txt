# ============================================
# RAG Pipeline - Python Dependencies
# ENHANCED VERSION with Hybrid Search, Reranking, Dashboard
# ============================================

# --------------------------------------------
# Airflow Core
# --------------------------------------------
apache-airflow==2.8.1
apache-airflow-providers-redis==3.5.0
apache-airflow-providers-postgres==5.10.0
apache-airflow-providers-amazon==8.16.0

# --------------------------------------------
# Vector Database
# --------------------------------------------
qdrant-client==1.7.0

# --------------------------------------------
# gRPC — pin both to avoid backtracking
# qdrant-client needs >=1.41.0; 1.62.x has wheels on
# all platforms and is compatible with protobuf>=5.
# --------------------------------------------
grpcio==1.62.3
grpcio-tools==1.62.3

# --------------------------------------------
# Protobuf — must be >=5.26.1 (opentelemetry-proto
# 1.43.0 requires it) and accepted by mlflow >=2.14.
# --------------------------------------------
protobuf>=5.26.1,<6.0.0

# --------------------------------------------
# Embeddings & LLMs
# --------------------------------------------
#openai==1.10.0
tiktoken==0.5.2
sentence-transformers==2.3.1

# --------------------------------------------
# Text Processing
# --------------------------------------------
langchain==0.1.4
langchain-community==0.0.16
pypdf==3.17.4
beautifulsoup4==4.12.3
lxml==5.1.0
markdownify==0.11.6

# --------------------------------------------
# Hybrid Search
# --------------------------------------------
rank-bm25==0.2.2

# --------------------------------------------
# HTTP & Web
# --------------------------------------------
requests==2.31.0
aiohttp==3.9.1

# --------------------------------------------
# Database Drivers
# --------------------------------------------
psycopg2-binary==2.9.9
redis==4.6.0

# --------------------------------------------
# MLOps & Monitoring
# MLflow >=2.14 drops the protobuf <5 upper bound,
# which was the root cause of the grpcio-tools build
# failure (pip backtracked all the way to 1.48.2, a
# source-only package that fails without pkg_resources).
# --------------------------------------------
mlflow==2.14.3
prometheus-client==0.19.0

# --------------------------------------------
# Machine Learning
# --------------------------------------------
scikit-learn==1.4.0
numpy==1.26.3

# --------------------------------------------
# Dashboard
# --------------------------------------------
streamlit==1.30.0

# --------------------------------------------
# Utilities
# --------------------------------------------
python-dotenv==1.0.0
pydantic==2.5.3
tenacity==8.2.3

# --------------------------------------------
# AWS SDK (Optional)
# --------------------------------------------
boto3==1.34.20
botocore==1.34.20

# --------------------------------------------
# Testing
# --------------------------------------------
pytest==7.4.4
pytest-cov==4.1.0
pytest-mock==3.12.0
setuptools>=65.0.0
groq==0.9.0